/** Colour tokens a habit can be tagged with. Mirrored in client/src/utils/habitTheme.js */
export const HABIT_COLORS = ['sage', 'clay', 'sky', 'lavender', 'amber', 'rose'];

/** How often a habit is meant to be performed. */
export const FREQUENCY_TYPES = ['daily', 'specific', 'weekly'];

/** Name of the auth cookie holding the session JWT. */
export const AUTH_COOKIE = 'ht_token';

/** Days of the week, Monday first — the grid order used throughout the UI. */
export const WEEK_DAYS = [1, 2, 3, 4, 5, 6, 0];
